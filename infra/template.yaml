AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Handshake contract registry (P2-owned stub). Region ap-south-1.

# Stub scaffold only. Resources (resolver/ingest/context/verdict Lambdas,
# API Gateway, DynamoDB contract table, Bedrock access) to be filled by P2.

Globals:
  Function:
    Runtime: nodejs20.x
    Timeout: 10
    MemorySize: 256

Resources: {}

Outputs: {}
